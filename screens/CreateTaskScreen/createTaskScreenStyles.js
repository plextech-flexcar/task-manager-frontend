import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    vstack: {
        marginBottom: 15,
        marginTop: 15,
        display: "flex",
        justifyContent: "space-around"
    },
});

export { styles };