import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  searchBar: {
    width: 382,
    height: '40px',
    backgroundColor: '#FFFFFF',
    left: '1px',
    borderWidth: '2px',
    borderRadius: '8px',
    borderColor: '#2A00A5',
    bottom: '16px',
    top: '1px',
    marginBottom: '16px',
  },
});
