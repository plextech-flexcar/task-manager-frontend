import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  searchBar: {
    width: 382,
    height: 40,
    backgroundColor: '#FFFFFF',
    left: 1,
    borderWidth: 2,
    borderRadius: 8,
    borderColor: '#2A00A5',
    bottom: 16,
    top: 1,
    marginBottom: 16,
  },
});
