import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    headerStyle: {
        display: "flex",
        flexDirection: "row",
    },
    backButton: {
        alignSelf: "flex-end",
        flexDirection: "row",
      }
});

export { styles };