import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  priorityView: {
    display: 'flex',
    alignContent: 'center',
    alignItems: 'center',
  },
  priorityImage: {
    width: 27,
    height: 27,
  },
})

export default styles;