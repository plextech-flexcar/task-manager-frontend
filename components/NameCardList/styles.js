import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  box: {
    width: '100%',
    borderWidth: '1%',
    borderStyle: 'solid',
    borderColor: 'white',
    backgroundColor: 'white',
    marginTop: '5%',

  },
});

export { styles };
