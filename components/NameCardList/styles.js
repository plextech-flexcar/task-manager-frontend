import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  box: {
    width: '100%',
    height: '110%',
    borderRadius: 10,
    borderWidth: '1px',
    borderStyle: 'solid',
    borderColor: 'white',
    top: "16px",
    backgroundColor: "white",    
    
  },
});

export { styles };
