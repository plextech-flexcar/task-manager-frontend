import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  box: {
    width: '90%',
    height: '100%',
    borderRadius: 10,
    borderWidth: '0.5%',
    borderStyle: 'solid',
    borderColor: 'white',
    backgroundColor: 'white',
    marginTop: '5%'
  },
});

export { styles };
